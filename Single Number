class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int l = nums.size();
        int sum = 0;
        for(int i=0;i<l;i++) sum = sum^nums[i];
        
        return sum;
    }
};
